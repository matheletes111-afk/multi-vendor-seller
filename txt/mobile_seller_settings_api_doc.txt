# MOBILE SELLER SETTINGS API DOCUMENTATION

This document provides definitive details for the Seller Settings / Profile management APIs.
Both Product and Service sellers follow the same pattern but under different base paths.

---

## 1. Base Paths & Endpoints
Depending on the application, use the appropriate base path for the seller type:
- **Product Seller**: `/mobileapi/product-seller/settings`
- **Service Seller**: `/mobileapi/service-seller/settings`

---

## 2. GET: Fetch Seller Details (Profile / Store / KYC)
**Endpoint**: `GET <Base_Path>`
**Auth**: Bearer Token (JWT with appropriate Seller Role)

### Full Response Example (200 OK):
```json
{
  "success": true,
  "data": {
    "id": "cm4d9...",
    "userId": "user123",
    "type": "PRODUCT",
    "onboardingCompleted": true,
    "status": "APPROVED",
    "adminFeedback": null,
    "isApproved": true,
    "isSuspended": false,
    "user": {
      "id": "user123",
      "name": "Jeet Basak",
      "email": "jeet@example.com",
      "image": "https://s3.url/profile.jpg",
      "phone": "+91 9876543210",
      "phoneCountryCode": "IN"
    },
    "store": {
      "name": "My Premium Store",
      "logo": "https://s3.url/logo.jpg",
      "banner": "https://s3.url/banner.jpg",
      "address": "123 Street",
      "city": "Kolkata",
      "state": "West Bengal",
      "zipCode": "700001",
      "lat": 22.5726,
      "lng": 88.3639
    },
    "businessInfo": {
      "businessName": "Meeem Solutions",
      "businessType": "Individual",
      "haveGst": true,
      "taxIdNumber": "GST12345",
      "busRegCertUrl": "https://s3.url/cert.pdf"
    },
    "bankDetails": {
      "bankName": "HDFC Bank",
      "accountNumber": "50100...",
      "branchName": "Salt Lake",
      "preferredPayoutMethod": "Bank Transfer"
    },
    "kyc": {
      "idType": "National ID",
      "idNumber": "1234-5678",
      "idFrontUrl": "...",
      "idBackUrl": "...",
      "selfieUrl": "..."
    },
    "selectedCategories": [
      { "id": "cm0c1x...", "name": "Electronics", "isActive": true }
    ],
    "// Note": "Service Sellers will see 'selectedServiceCategories' instead of 'selectedCategories'"
  }
}
```

---

## 3. PUT: Update Settings (Hybrid Payload)
**Endpoint**: `PUT <Base_Path>`
**Auth**: Bearer Token (JWT)

You can send the payload as **application/json** or **multipart/form-data** depending on whether you need to upload files.

### 📋 Part 1: User Profile Settings
Primary account holder information.

| Field Name | Format | Required | Description |
| :--- | :--- | :--- | :--- |
| `user.name` (JSON) / `name` (Form) | Text | No | Full name of the seller. |
| `user.phone` (JSON) / `phone` (Form) | Text | No | Primary contact number. |
| `user.phoneCountryCode` (JSON) / `phoneCountryCode` (Form)| Text | No | Country prefix (e.g., +1, +91). |
| `user.password` (JSON) / `password` (Form) | Text | No | New password (min 6 characters). |
| `seller.nationIdentityNumber` (JSON) / `nationIdentityNumber` (Form) | Text | **Yes** | 11-digit National Identity Number (NIN). Required. |
| `profileImage` | Multipart File| No | User's profile photo. |

### 📊 Part 2: Store Information
Public-facing store identity and location.

| Field Name | Format | Required | Description |
| :--- | :--- | :--- | :--- |
| `store.name` (JSON only) | Text | No | Name of the store. |
| `store.description` (JSON only) | Text | No | Short bio or description. |
| `store.phone` (JSON only) | Text | No | Public customer service number. |
| `store.website` (JSON only) | Text | No | Official store URL. |
| `store.city` (JSON only) | Text | No | Store city. |
| `store.state` (JSON only) | Text | No | Store state/province. |
| `store.zipCode` (JSON only) | Text | No | Postal code. |
| `store.country` (JSON only) | Text | No | Country name. |
| `store.address` (JSON) / `storeAddress` (Form) | Text | No | Full street address. |
| `store.lat` (JSON) / `storeLat` (Form) | Float/Text | No | Map latitude. |
| `store.lng` (JSON) / `storeLng` (Form) | Float/Text | No | Map longitude. |
| `storeLogo` | Multipart File| No | Upload store logo. |
| `storeBanner` | Multipart File| No | Upload store banner. |
| `categoryIds` | Array (JSON) / Multiple (Form) | No | List of selected category IDs. |

### 🏢 Part 3: Business & Tax Information
Legal entity details and GST configuration.

| Field Name | Format | Required | Description |
| :--- | :--- | :--- | :--- |
| `seller.businessInfo.businessName` (JSON) / `businessName` (Form) | Text | No | Registered legal name. |
| `seller.businessInfo.businessType` (JSON) / `businessType` (Form) | Text | No | e.g., Proprietor, Partnership. |
| `seller.businessInfo.businessRegNumber` (JSON) / `businessRegNumber` (Form) | Text | No | Registration ID. |
| `seller.businessInfo.haveGst` (JSON) / `haveGst` (Form) | Bool/Text | No | "true" or "false". Label: "Do you sell with GST?" |
| `seller.businessInfo.taxIdNumber` (JSON) / `taxIdNumber` (Form) | Text | Yes | Tax Identification Number (TIN). Always required. |
| `seller.businessInfo.gstCustomerName` (JSON) / `gstCustomerName` (Form) | Text | No | Customer Name for GST. |
| `seller.businessInfo.gstInvNo` (JSON) / `gstInvNo` (Form) | Text | No | Invoice Number for GST. |
| `busRegCert` | Multipart File| No | Upload Registration Doc. |

### 🏦 Part 4: Bank & Payout Details
Financial records for receiving payments.

| Field Name | Format | Required | Description |
| :--- | :--- | :--- | :--- |
| `seller.bankDetails.bankName` (JSON only) | Text | No | Name of the bank. |
| `seller.bankDetails.accountHolderName` (JSON only) | Text | No | Name on the account. |
| `seller.bankDetails.accountNumber` (JSON only) | Text | No | Bank account number. |
| `seller.bankDetails.branchName` (JSON only) | Text | No | Branch location/code. |
| `seller.bankDetails.preferredPayoutMethod` (JSON only) | Text | No | Currently only 'Bank Transfer' is supported. |
| `bankPassbook` | Multipart File| No | Upload Passbook/Cheque. |

### 🆔 Part 5: KYC & Identity
Seller verification documents.

| Field Name | Format | Required | Description |
| :--- | :--- | :--- | :--- |
| `seller.kyc.idType` (JSON only) | Text | No | e.g., National ID, Passport. |
| `seller.kyc.idNumber` (JSON only) | Text | No | ID document number. |
| `idFront` | Multipart File| No | Upload ID Front. |
| `idBack` | Multipart File| No | Upload ID Back. |
| `selfie` | Multipart File| No | Upload Selfie with ID. |

### 🏷️ Part 6: Categories
| Field Name | Format | Required | Description |
| :--- | :--- | :--- | :--- |
| `seller.categoryIds` (JSON) / `categoryIds` (Form) | Array / Multi | No | Array of category IDs. |

### 🚀 Part 7: Example Request (Full JSON)

```json
{
  "user": {
    "name": "Jane Doe",
    "phone": "1234567890",
    "phoneCountryCode": "+1"
  },
  "store": {
    "name": "Jane's Emporium",
    "address": "123 Main St",
    "lat": 10.123,
    "lng": 11.456
  },
  "seller": {
    "categoryIds": ["cat_1", "cat_2"],
    "businessInfo": {
      "haveGst": true,
      "taxIdNumber": "GST-12345"
    },
    "bankDetails": {
      "preferredPayoutMethod": "Bank Transfer",
      "bankName": "Central Bank"
    }
  }
}
```

### Full Success Response (200 OK) for PUT Request:
```json
{
  "success": true,
  "message": "Settings updated successfully"
}
```

---

## 4. Error Responses Reference

### 400 Bad Request
```json
{ "success": false, "error": "Phone number already in use" }
```

### 401 Unauthorized
```json
{ "success": false, "error": "Unauthorized" }
```

### 403 Forbidden
```json
{ "success": false, "error": "Forbidden" }
```

### 404 Not Found
```json
{ "success": false, "error": "Seller not found" }
```
