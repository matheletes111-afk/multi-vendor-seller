import { ProductOnboardingClient } from "./onboarding-client"

export const metadata = {
  title: "Seller Onboarding | Meeem",
  description: "Complete your seller profile to start selling products on the Meeem marketplace.",
}

export default function OnboardingPage() {
  return <ProductOnboardingClient />
}
