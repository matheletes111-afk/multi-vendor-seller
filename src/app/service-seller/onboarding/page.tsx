import { ServiceOnboardingClient } from "./onboarding-client"

export const metadata = {
  title: "Service Seller Onboarding | Meeem",
  description: "Register as a service provider and reach more customers on the Meeem platform.",
}

export default function OnboardingPage() {
  return <ServiceOnboardingClient />
}
